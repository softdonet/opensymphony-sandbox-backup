Run:

mvn idea:idea

Finally, open able.ipr

Note: Normally the project files generated by "mvn idea:idea" are *updated* rather than over-written.
      If you wish to overwrite the files, you can do "mvn -Doverwrite=true idea:idea".

